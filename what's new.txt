# Changes:

  # To use full path for imu_port in launch file

  <!-- set IMU port -->
  <arg name="imu_port" default="/dev/ttyUSB2"/>

# Supports serial ctrl:

  # adding following entry into launch file
   
  <!-- set Serial port -->
  <arg name="serial_port" default="/dev/ttyUSB0"/>
  
  ...
  
      <param name="serial_port" value="$(arg serial_port)"/>
      
      
